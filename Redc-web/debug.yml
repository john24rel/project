- hosts: db
  tasks:
  - name: Debug
    debug:
      msg: "{{ ansible_os_family }}"


- hosts: db
  tasks:
  - name: Debug
    debug:
      msg: "{{ service_name }}"

- hosts: web
  tasks:
  - name: Debug
    debug:
      msg: "{{ service_name }}"


- hosts: web
  tasks:
  - name: Debug
    debug:
      msg: "{{ ansible_os_family }}"
